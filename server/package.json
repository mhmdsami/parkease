{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/utils/migrate.ts",
    "cron": "bun run src/utils/cron.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.3.0",
    "drizzle-orm": "^0.33.0",
    "drizzle-zod": "^0.5.1",
    "hono": "^4.6.3",
    "node-cron": "^3.0.3",
    "postgres": "^3.4.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node-cron": "^3.0.11",
    "drizzle-kit": "^0.24.2"
  }
}